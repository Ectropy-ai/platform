#!/bin/bash
# Deploy to production server (blue/green)
# Referenced by: build-production-images.yml (summary text only, not executed)
# Actual deploy uses: scripts/deployment/deploy-to-server.sh
echo "Usage: bash scripts/deployment/deploy-to-server.sh [blue|green]"
echo "This script is a placeholder — use deploy-to-server.sh instead."
exit 0
