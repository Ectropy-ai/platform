name: Check Production Env (Read-Only)

on:
  workflow_dispatch:

jobs:
  check-env:
    runs-on: ubuntu-latest
    steps:
      - name: Install doctl
        uses: digitalocean/action-doctl@v2
        with:
          token: ${{ secrets.DIGITALOCEAN_ACCESS_TOKEN }}

      - name: Setup SSH key
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.PROD_SSH_KEY }}

      - name: Add runner IP to firewall (temporary)
        id: add-firewall-rule
        run: |
          RUNNER_IP=$(curl -s https://api.ipify.org)
          echo "Runner IP: $RUNNER_IP"
          echo "runner_ip=$RUNNER_IP" >> $GITHUB_OUTPUT
          FIREWALL_ID="${{ vars.PROD_FIREWALL_ID }}"
          doctl compute firewall add-rules $FIREWALL_ID \
            --inbound-rules "protocol:tcp,ports:22,address:$RUNNER_IP/32"
          echo "Waiting 5s for firewall propagation..."
          sleep 5

      - name: Add SSH known hosts
        run: |
          ssh-keyscan -H ${{ vars.PROD_BLUE_IP }} >> ~/.ssh/known_hosts
          ssh-keyscan -H ${{ vars.PROD_GREEN_IP }} >> ~/.ssh/known_hosts

      - name: Check Blue server env
        run: |
          echo "=== BLUE (${{ vars.PROD_BLUE_IP }}) ==="
          ssh -o ConnectTimeout=15 root@${{ vars.PROD_BLUE_IP }} bash -s <<'REMOTE'
          echo "--- 1. OAuth keys in /opt/ectropy/.env ---"
          grep 'GOOGLE_CLIENT_SECRET\|GOOGLE_CLIENT_ID\|OAUTH_CALLBACK_URL' /opt/ectropy/.env | sed 's/=.*/=<REDACTED>/' || echo "(not found)"

          echo "--- 2. stat /opt/ectropy/.env ---"
          stat /opt/ectropy/.env 2>&1 || echo "(file does not exist)"

          echo "--- 3. Key name prefixes (first 40 chars) ---"
          grep 'GOOGLE_CLIENT_SECRET\|GOOGLE_CLIENT_ID' /opt/ectropy/.env | cut -c1-40 | sed 's/=.*/=<REDACTED>/' || echo "(not found)"

          echo "--- 4. Container env (for comparison) ---"
          docker exec ectropy-api-gateway env 2>/dev/null | grep -E 'GOOGLE_CLIENT|OAUTH_CALLBACK_URL|SESSION_SECRET' | sed 's/=.*/=<REDACTED>/' || echo "(container not running)"
          REMOTE

      - name: Check Green server env
        run: |
          echo "=== GREEN (${{ vars.PROD_GREEN_IP }}) ==="
          ssh -o ConnectTimeout=15 root@${{ vars.PROD_GREEN_IP }} bash -s <<'REMOTE'
          echo "--- 1. OAuth keys in /opt/ectropy/.env ---"
          grep 'GOOGLE_CLIENT_SECRET\|GOOGLE_CLIENT_ID\|OAUTH_CALLBACK_URL' /opt/ectropy/.env | sed 's/=.*/=<REDACTED>/' || echo "(not found)"

          echo "--- 2. stat /opt/ectropy/.env ---"
          stat /opt/ectropy/.env 2>&1 || echo "(file does not exist)"

          echo "--- 3. Key name prefixes (first 40 chars) ---"
          grep 'GOOGLE_CLIENT_SECRET\|GOOGLE_CLIENT_ID' /opt/ectropy/.env | cut -c1-40 | sed 's/=.*/=<REDACTED>/' || echo "(not found)"

          echo "--- 4. Container env (for comparison) ---"
          docker exec ectropy-api-gateway env 2>/dev/null | grep -E 'GOOGLE_CLIENT|OAUTH_CALLBACK_URL|SESSION_SECRET' | sed 's/=.*/=<REDACTED>/' || echo "(container not running)"
          REMOTE

      - name: Remove runner IP from firewall (cleanup)
        if: always() && steps.add-firewall-rule.outputs.runner_ip
        run: |
          RUNNER_IP="${{ steps.add-firewall-rule.outputs.runner_ip }}"
          FIREWALL_ID="${{ vars.PROD_FIREWALL_ID }}"
          doctl compute firewall remove-rules $FIREWALL_ID \
            --inbound-rules "protocol:tcp,ports:22,address:$RUNNER_IP/32" || true
