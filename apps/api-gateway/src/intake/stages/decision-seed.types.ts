/**
 * @fileoverview Re-export stub — canonical types live in @ectropy/schemas.
 * Moved during F-2 schema extraction (2026-04-18).
 * @see ectropy-ai/schemas/types/decision-seed.types.ts
 */

export type {
  ClashLocation,
  DecisionExposure,
  DecisionAuthority,
  DecisionPreApproval,
  AIAnalysis,
  DecisionDef,
  DecisionSeedFile,
} from '@ectropy/schemas/types/decision-seed';
